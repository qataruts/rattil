# مشروع رَتِّلْ — التجويد (قبل بوابة التنفيذ)

تطبيق تعليم التجويد لطفلٍ أتمّ «اقرأ» — **فاتحُ الفرع القرآني** (رتل ثم احفظ)،
سابعُ العائلة عملاً. قرارُ المالك (١٦ أغسطس ٢٠٢٦): **رتل قبل اتقن ١** — فائدةٌ
قريبة لخرّيج اقرأ الواقفِ على بابه، وصفرُ تزاحمٍ مع الإنكليزية الجارية (صوتُه
تلاوةٌ تُجلب لا TTS يولَّد)، واتقن ١ (الإملاء) موعدُه مطلعُ المدرسة.

## الحالة: بوابة الدراسة مفتوحة (أمر المالك، ١٦ أغسطس ٢٠٢٦)

- **الدراسة**: ✅ مفتوحة — منتَجُها `docs/STUDY.md` على تكليف `docs/STUDY_BRIEF.md`،
  ويُرفع لمدير المجموعة ثم المالك.
- **التخطيط**: بعد اعتماد الدراسة — `PLAN.md` ثم `METHOD.md` للاعتماد.
- **التنفيذ**: 🔒 مقفل حتى يعتمد المالك `METHOD.md` — لا شيفرة قبله.
- **الهوية**: الاسم **«رَتِّلْ»** مقرَّرٌ سلفاً (سياسة التسمية:
  `../read/ref/sonnet-con/naming.md` — Rattil اصطلاحٌ إسلاميّ راسخ)، والنطاق
  `rattil.mishkat.qa`. اللوحُ والأيقونة وخطُّ العلامة قراراتُ مالكٍ بمرشَّحات
  مُصيَّرة (ميثاق `../calc/docs/FAMILY.md §٩`) يومَ بوابتها.
- **الصوت**: لا قرارَ صوتٍ جديد — التلاوةُ بصوت قارئ (بنكُ الحصري القائم في اقرأ
  ومسارُ `fetch_recitation.py`)، وتعليماتُه العربية بصوت سُلافات من خط اقرأ
  القائم (عبر `tools/audio_queue.json` كالعادة).
- **مدير المجموعة**: جلسة إدارة «اقرأ» — إليها تُرفع المنتَجات وعندها البوابات.

## عهود رتل الخاصة (من ولايته — `docs/MANDATE.md`)

- **نسخةٌ واحدة متّسعة لا مستويات**: شكلُ التمرين ثابت وتتبدل **القاعدة**
  (معيار `FAMILY.md §١٠`).
- **لا تمييزَ صوتٍ حيّ أبداً** — حدُّ نطاقٍ معلَن من اليوم الأول: الطفل يسمع
  القارئ ويميّز ويختار **ويسجّل لنفسه ولوالده** بكتلة record المعهودة —
  والتطبيق لا يحكم على نطقه، ولا يُوعَد أحدٌ بغير ذلك.
- **مواضعُ الأحكام لا تُكتب بيد**: تُشتقّ من بيان `/Volumes/data/new-projects/quran`
  المتحقَّق (`qpc-hafs-tajweed.json` مواضعُ الأحكام كلمةً كلمة + spans التوقيت
  المستعملة في اقرأ اليوم)، **وفاحصٌ يرفض حكماً ليس في البيان**.
- **نصُّ المصحف** من `../read/tools/quran_source.txt` حصراً — لا يُكتب بيدنا
  ولا يُنسخ من شاشة، ولا يُنطق آلياً أبداً.
- **المرجع المنهجي الأول**: خواتيمُ القاعدة النورانية والدروس الهجائية
  (`../read/ref/` — الكتابان PDF): منهجُنا الأمُّ يختم بالتجويد، وخرّيجُنا
  يعرف اصطلاحَه.
- **الربط مع احفظ مؤجَّل بحكمٍ مبدئي**: توصيةٌ بصرية لا قفل، وv1 بلا ربط —
  القرار النهائي يومَ يُبنى الثاني (خطة المسار §٤).

## قواعدُ عائلةٍ قائمة (تسري يومَ يُبنى — من بريد العائلة وFAMILY.md §١٠)

- قناةُ الصوت تُنسخ **بإصلاحاتها الثلاثة** (epoch/_hush + quiet/afterSpeech +
  onScreen) من مصدرها المُصلَح بمرجع التزامه.
- **لا فعلَ يلزم الطفلَ وسيلتُه الوحيدة قراءة** (الجمهور قبل-قارئ للنص العاري).
- **البرومبت يُسلَّم كاملاً غيرَ مُجمَّع**، وتعديلُه «ألغِ السابق وهذا بديله».
- سقفُ الحلقات في المجموعة ١٠–١٢ (**سقفٌ لا هدف — والأقل صحّي**).
- **لا يُدفَع أحمر**؛ والفحصُ بقدر العمل أثناءه وكاملٌ عند بابَي الرفع والنشر.
- قواعدُ وقت الجلسات الأربع، وجلساتُ المستودع الواحد **تسلسلاً**.
- الإغلاق بشرطَيه: **مراجعةٌ منهجيةٌ مستقلة** نافذةُ الأحكام + **امتحانُ طفلٍ**
  ميدانيّ موثَّق.
- «بلّغنا» من لوحة الوالد: واتساب `+97433882806` وبريد `info@mishkat.qa`،
  والنسخةُ ظاهرة في اللوحة.

## خريطة الملفات

- `docs/MANDATE.md` — **الولاية**: نصُّ بند رتل من خطة المسار المتقدم
  (`../family/quran-track-plan.md`) وقرارُ الترتيب — الخطةُ المرجعية داخل المستودع.
- `docs/STUDY_BRIEF.md` — تكليف جلسة الدراسة (المرجع الملزم لها).
- `docs/STUDY.md` — منتَج الدراسة (لم يُكتب بعد).
- `docs/FAMILY_MAIL.md` — سجل إيصالات بريد العائلة.

## مراجع العائلة (تُقرأ ولا تُمَسّ)

- `../calc/docs/FAMILY.md` — الخطة العامة والمبادئ **وميثاق الهوية §٩** و§١٠.
- `../family/quran-track-plan.md` — خطة المسار المتقدم (أصل الولاية).
- `../read/` — التطبيق الأم: المحرّك المرجعي وعهودُه، بنكُ الحصري،
  و`ref/` الكتابان المرجعيان.
- `/Volumes/data/new-projects/quran` — **الرافد البياني**: بيان التجويد
  المتحقَّق وspans التوقيت (يُقرأ ولا يُمَسّ).

## بريد العائلة — لجلسة الإدارة وحدها

- البريدُ شأنُ **جلسة الإدارة (فابل) وحدَها** — جلساتُ التنفيذ لا تقرؤه ولا
  تكتب فيه.
- إن كنتَ جلسةَ الإدارة: في **أول كل جلسة** اقرأ ما استجدّ في
  `../family/bulletins/` (عقدُ البريد في `../family/README.md`)، وقيّد إيصالَ
  كل بلاغٍ في `docs/FAMILY_MAIL.md`. **بلاغُ إجراءٍ لا يُغلق إلا بإيصاله.**
  (وخطّافُ `UserPromptSubmit` في `.claude/settings.json` ينبّه آلياً لغير المُوصَل.)
- وبلاغُك للعائلة يُكتب ملفاً هناك ويُلتزم — لا يُنقل عبر المالك.
